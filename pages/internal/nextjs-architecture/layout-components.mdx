# Layout Components

Layout components render `SASCs`, after they are passed through their respective `CALCs`. Remember, `CALCs` compose the `SASCs`, but contain no semantic or styling data themselves (the styled-components are contained within the child `SASCs` only).
